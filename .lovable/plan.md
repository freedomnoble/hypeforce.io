Change the hero tagline in `src/components/hypeforce/landing-page.tsx` (line 114) from a `<p>` to an `<h2>`, keeping the existing classes (`text-xl sm:text-2xl text-foreground/90 font-display tracking-tight`) and text unchanged. No other changes.